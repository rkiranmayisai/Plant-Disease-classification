// ================================================
// PlantAI - Home Page Specific Features
// ================================================

document.addEventListener('DOMContentLoaded', () => {
  initHomePage();
});

function initHomePage() {
  // Check if we want to add any special hover features,
  // floating graphics coordination, or analytics charts on the homepage.
  
  // Showcase section - automatic transition of crops
  const showcaseTabs = document.querySelectorAll('.disease-tab');
  if (showcaseTabs.length > 0) {
    let activeIdx = 0;
    // We could automatically transition tabs for demo effect,
    // but standard click handlers in lang.js are already robust!
  }
}
